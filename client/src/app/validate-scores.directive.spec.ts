import { ValidateScoresDirective } from './validate-scores.directive';

describe('ValidateScoresDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidateScoresDirective();
    expect(directive).toBeTruthy();
  });
});
